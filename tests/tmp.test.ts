import { describe } from 'vitest'
import { formatCase } from './helpers/format-case'

/** 本地临时调试，用 npm run test-tmp 运行 */
describe('scratch', () => {
  formatCase({
    name: 'tmp',
    input: `
`,
  })
})
